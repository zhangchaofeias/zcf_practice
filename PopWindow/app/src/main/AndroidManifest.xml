<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.huoyunren.popwindow"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".ALifeCycle" />
        <activity android:name=".ALifeCycleTwo" />
        <activity android:name=".BBroadCastExample" />
        <activity android:name=".CServiceExample" />

        <service
            android:name=".CMyService"
            android:enabled="true"
            android:exported="true">
        </service>

        <activity android:name=".InputMothod">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InputMethodTwo">
        </activity>
    </application>

</manifest>